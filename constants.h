#ifndef CONSTANTS_H
#define CONSTANTS_H

const int SCREEN_WIDTH = 1280;
const int SCREEN_HEIGHT = 720;

// user
const int DOT_SIZE = 20;
const float MAX_SPEED = 200.0f; // Максимальная скорость (пикселей в секунду)
const float ACCELERATION = 200.0f; // Ускорение (пикселей в секунду^2)
const float DECELERATION = 100.0f; // Замедление (пикселей в секунду^2)

// enemy
const int ENEMY_SIZE = 20;
const float SPEED = 100.0f;

#endif
